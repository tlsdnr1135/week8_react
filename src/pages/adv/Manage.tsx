import React, { useEffect, useState } from 'react';
import { Outlet, Route, useNavigate } from 'react-router-dom';

export const Manage = () => {
    return (
        <>
            <Outlet></Outlet>
        </>
    );
};
